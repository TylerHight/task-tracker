import { describe, expect, it } from "vitest";
import { beginGoogleAuthorization, consumeGoogleAuthorization, decryptRefreshToken, encryptRefreshToken, grantedScopes, revokeGoogleCredential, storeGoogleCredential } from "@/server/auth/google";
describe("Google credential security", () => { it("round-trips without exposing plaintext in ciphertext", () => { const ciphertext = encryptRefreshToken("refresh-token"); expect(ciphertext).not.toContain("refresh-token"); expect(decryptRefreshToken(ciphertext)).toBe("refresh-token"); }); it("requires matching PKCE state and stores only server-side scopes", () => { const attempt = beginGoogleAuthorization(["openid", "calendar.events"]); expect(consumeGoogleAuthorization(attempt.state, attempt.codeVerifier)).toEqual(["openid", "calendar.events"]); storeGoogleCredential("user", "refresh-token", ["calendar.events"]); expect(grantedScopes("user")).toEqual(["calendar.events"]); revokeGoogleCredential("user"); expect(grantedScopes("user")).toEqual([]); }); });
